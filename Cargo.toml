[package]
name = "slack_emojify"
version = "0.1.2"
edition = "2024"
description = "A quick and dirty Slack emoji to Unicode emoji converter."

[dependencies]
regex = "1.10"
serde_json = "1.0"

[workspace]
members = ["xtask"]
